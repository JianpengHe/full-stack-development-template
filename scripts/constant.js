const path = require('path')

const PROJECT_PATH = path.resolve(__dirname, '../') // 项目根路径

const SERVER_HOST = 't.hejianpeng.com'
const SERVER_PORT = 25918

module.exports = {
  PROJECT_PATH,
  SERVER_HOST,
  SERVER_PORT,
}
